/* ==========================================================================
   Конфигурация по умолчанию.
   Это только "стартовый набор" — при первом запуске он копируется в
   config.json внутри выбранной вами папки с данными, и дальше уже
   config.json (а не этот файл) хранит актуальный список: добавленные вами
   вручную виды документов и контрагенты сохраняются именно туда.

   Вид документа — это одновременно и его короткое обозначение (префикс),
   из которого собирается отображаемое имя документа и имя файла вложения:
     вид "УПД" + номер "2594" + дата "31.07.26" → "УПД №2594 от 31.07.26"

   Расшифровки (для справки, в самом конфиге не хранятся):
     УПД — универсальный передаточный документ
     АКТ — акт выполненных работ / оказанных услуг
     ТН  — товарная накладная
   ========================================================================== */

const DEFAULT_CONFIG = {
  docTypes: ["УПД", "АКТ", "ТН"],
  counterparties: [],
};
